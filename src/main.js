import React from 'react';
import ReactDOM  from 'react-dom';

import App from './App.js';
import 'foundation-dist-css';

const root = document.getElementById('app-root');

ReactDOM.render(<App />, root);
